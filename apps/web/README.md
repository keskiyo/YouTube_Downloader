# Web App

Vite + React frontend for the video downloader.

Run from the repository root:

```bash
bun run dev:web
```

The development server runs on `http://localhost:5173` and proxies `/api/*` to the backend on `http://localhost:3001`.
